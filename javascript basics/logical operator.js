// and operator &&
// or operator ||
// not operator !

const hasDrivingLicence=true;
const hasGoodVision=true;
const isTired=false;

if(hasDrivingLicence && hasDrivingLicence  && !isTired){
 console.log("Can drive")
}else{
    console.log("can't  drive")
}
