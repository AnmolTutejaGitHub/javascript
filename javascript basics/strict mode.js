'use srtict'

let hasDriversLicence=false;
const passTest=true;

if(passTest) hasDriverLicence=true; //intentially spelling mistake
if(hasDriversLicence) console.log("I can drive")


//strict mode prevents us from defining variables that can be keywords in future
const interface='Audio';